name: Build and Release Forge Mod

on:
  push:
    branches:
      - main
    tags:
      - 'v*.*.*' # Detecta versiones como v1.0.0

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # - name: Checkout repository
      #   uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      # - name: Set up Gradle
      #   uses: gradle/gradle-build-action@v2

      # - name: Grant execute permission to Gradle wrapper
      #   run: chmod +x gradlew

      # - name: Install git-cliff
      #   run: npm install -g git-cliff

      # - name: Generate changelog
      #   run: git-cliff -o CHANGELOG.md

      # - name: Build with Gradle
      #   run: ./gradlew build

      # - name: Run JarJar
      #   run: ./gradlew jarjar

      # - name: Find Jar File
      #   id: find_jar
      #   run: echo "JAR_FILE=$(ls build/libs/*.jar | head -n 1)" >> $GITHUB_ENV

      # - name: Upload artifacts
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: ForgeMod
      #     path: build/libs/*.jar
  
      - name: Get Gradle Version
        id: get_gradle_version
        uses: ./.github/actions/get-gradle-property #  <-- Ruta a la acción creada

        #O bien, si usa script inline:
        #run: |
        #  PROPERTY_VALUE=$(./gradlew -q properties | grep "^versionName:" | awk -F ': ' '{print $2}')
        #  echo "::set-output name=version::$PROPERTY_VALUE"
        with:
          property-name: versionName  # Reemplaza con el nombre de tu propiedad

      # Usar la propiedad en pasos posteriores
      - name: Display Gradle Version
        run: echo "Gradle Version:${{ steps.get_gradle_version.outputs.value }}"
          
  release:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Mod Version
        run: |
          echo "${{ steps.all.outputs.mod_version }}"
    
      # - name: Checkout repository
      #   uses: actions/checkout@v4

      # - name: Download built JAR
      #   uses: actions/download-artifact@v4
      #   with:
      #     name: ForgeMod
      #     path: build/libs

      # - name: Tag
      #   run: |
      #     git tag "${{ env.MOD_VERSION_OUTPUT }}"
      #     git push origin "${{ env.MOD_VERSION_OUTPUT }}"

      # - name: Create GitHub Release
      #   uses: softprops/action-gh-release@v2
      #   with:
      #     tag_name: ${{ env.MOD_VERSION_OUTPUT }}
      #     files: build/libs/*.jar
      #     body_path: CHANGELOG.md
      #     token: ${{ secrets.GITHUB_TOKEN }}
